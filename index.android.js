import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

var deco = require('./common');

AppRegistry.registerComponent('deco', () => deco);
